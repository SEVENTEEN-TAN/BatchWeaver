spring:
  application:
    name: batch-scheduler
  batch:
    jdbc:
      initialize-schema: never
    job:
      enabled: false

  # 多数据源配置（已优化 - 基于 Codex 权威分析）
  datasource:
    # DB1: Primary (Spring Batch 元数据 + 业务数据)
    db1:
      url: jdbc:sqlserver://124.223.172.114:1433;databaseName=DB1;encrypt=true;trustServerCertificate=true
      username: sa
      password: YourStrong!Passw0rd
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      hikari:
        maximum-pool-size: 15  # 优化：5 → 15
        minimum-idle: 5         # 优化：1 → 5
        connection-timeout: 10000  # 优化：30s → 10s

    # DB2: Business Only
    db2:
      url: jdbc:sqlserver://124.223.172.114:1433;databaseName=DB2;encrypt=true;trustServerCertificate=true
      username: sa
      password: YourStrong!Passw0rd
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      hikari:
        maximum-pool-size: 10  # 优化：5 → 10
        minimum-idle: 3         # 优化：1 → 3
        connection-timeout: 10000  # 优化：30s → 10s

    # DB3: Business Only
    db3:
      url: jdbc:sqlserver://124.223.172.114:1433;databaseName=DB3;encrypt=true;trustServerCertificate=true
      username: sa
      password: YourStrong!Passw0rd
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      hikari:
        maximum-pool-size: 10  # 优化：5 → 10
        minimum-idle: 3         # 优化：1 → 3
        connection-timeout: 10000

    # DB4: Business Only
    db4:
      url: jdbc:sqlserver://124.223.172.114:1433;databaseName=DB4;encrypt=true;trustServerCertificate=true
      username: sa
      password: YourStrong!Passw0rd
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      hikari:
        maximum-pool-size: 10  # 优化：5 → 10
        minimum-idle: 3         # 优化：1 → 3
        connection-timeout: 10000

logging:
  level:
    root: INFO
    com.example.batch: INFO  # 优化：DEBUG → INFO
    org.springframework.batch: INFO
    org.springframework.jdbc.core: WARN  # 优化：DEBUG → WARN
    com.zaxxer.hikari: INFO
